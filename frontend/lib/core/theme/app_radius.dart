/// Corner radius tokens from the approved UI mockup.
abstract final class AppRadius {
  /// Standard card radius, also used by primary CTA buttons and any
  /// container that visually behaves like a card.
  static const double card = 14.0;
}
